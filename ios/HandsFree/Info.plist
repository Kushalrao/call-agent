<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>hands-free</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>

	<!-- Required: the app records audio. Spec 2.1. -->
	<key>NSMicrophoneUsageDescription</key>
	<string>hands-free needs your microphone so the other person can hear you on a call.</string>

	<!-- Audio-only app, but these must still be declared. LiveKit's WebRTC
	     enumerates capture devices, and iOS kills the process with
	     __abort_with_payload the instant a protected resource is touched with no
	     usage description present - it does not wait for a permission prompt. -->
	<key>NSCameraUsageDescription</key>
	<string>hands-free does not use video. This is required by the calling framework.</string>
	<key>NSBluetoothAlwaysUsageDescription</key>
	<string>hands-free routes call audio to Bluetooth headsets when one is connected.</string>
	<key>NSBluetoothPeripheralUsageDescription</key>
	<string>hands-free routes call audio to Bluetooth headsets when one is connected.</string>

	<!-- Background Modes. `audio` keeps call audio alive when backgrounded and
	     needs no entitlement. Spec 2.1.

	     `voip` is REMOVED for now. It was declared ahead of PushKit (Phase 7),
	     but nothing in this app uses PushKit yet - ringing is a WebSocket, see
	     EventSocket.swift. Declaring it requires a PushKit entitlement that a
	     free provisioning profile cannot grant, and recent iOS terminates the app
	     at launch rather than ignoring it: __abort_with_payload on Thread 1 before
	     any of our code runs. Costs nothing to drop, because the capability it
	     reserved is unimplemented.

	     Put it back in the same commit that adds PKPushRegistry, and only with a
	     paid team's provisioning profile. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>audio</string>
	</array>

	<!-- Dev only: the control plane runs on a Mac on the same Wi-Fi, over plain
	     HTTP on a LAN address. NSAllowsLocalNetworking permits exactly that and
	     nothing else - it does not disable ATS for the public internet. Remove
	     both keys once the backend is behind TLS. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<key>NSLocalNetworkUsageDescription</key>
	<string>hands-free connects to the development server running on your Mac.</string>
</dict>
</plist>
